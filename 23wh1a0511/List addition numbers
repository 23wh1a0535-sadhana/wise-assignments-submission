#addition of numbers in list
def addition():
    add = 0
    numbers = [1,34,65,34,22,12,24,6,78]
    for i in range (0,len(numbers)):
        add = add + numbers[i]
    print(f"sum of numbers in list = {add}")
addition()
